import React from 'react';
import { NavLink } from 'react-router-dom';

const Navigation = () => {
	return (
		<div>
			<NavLink to='/'>Create Poll</NavLink>
		</div>
	);
};

export default Navigation;
